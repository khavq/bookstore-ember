{{#if showAll}}
  <button {{action 'showAll'}}>Show All</button>
{{/if}}
{{!input value=value key-up=(action 'filteredArticles') class="light" placeholder="Filter By City"}}
{{!input value=value class="light" placeholder="Search..."}}
<!-- <button {{action 'filteredArticles'}}>Show All</button> -->
<!-- <ul>
  {{#each model as |book|}}
    {{book-cover book=book blurBackground=(route-action "blurBackground")}}
  {{/each}}

  {{infinity-loader infinityModel=model}}
  {{!page-numbers content=content}}
</ul> -->
<!-- <div id="paper-dialog-demo"> -->
<div class="layout-row layout-column-md layout-wrap " >
    <!-- <div class="layout-column flex-xs flex-gt-xs-50"> -->
   	
    	{{#each model as |book|}}
      <!-- <div class="layout-row layout-xs-column">  -->
        <div class="flex-30 flex-gt-xs-20">
          {{book-cover book=book blurBackground=(route-action "blurBackground")}}  
        </div>
	    <!-- </div>	 -->
	  	{{/each}}
	  {{infinity-loader infinityModel=model}}

    <!-- </div> -->
</div>
<!-- </div> -->