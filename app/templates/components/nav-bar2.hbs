<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{link-to "Bookstore" 'book' class='navbar-brand'}}
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <li>
          {{link-to "Dashboard" 'dashboard' class='nav-item nav-link'}}
        </li>
        <li>
          <a href="#" class="nav-item nav-link">Settings</a>
        </li>
        <li>
          {{#if session.isAuthenticated}}
            <a {{action 'invalidateSession'}}>Logout {{currentMember.member.email}}</a>

          {{else}}
            {{#link-to 'login'}}Login{{/link-to}}
          {{/if}}
        </li>
      </ul>
      <input class="navbar-form navbar-right" type="text" id="txt_search" class="form-control" placeholder="Search...">
    </div>
  </div>
</nav>